package com.example.transaction_validation;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MySpringbokAppApplication {

    public static void main(String[] args) {
        // This is the line that actually starts the Spring application context
        SpringApplication.run(MySpringbokAppApplication.class, args);
    }
}
